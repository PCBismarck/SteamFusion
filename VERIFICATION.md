# 验证范围

本文件使用通用描述；原始本机日志、账号信息、配置、游戏数据和截图不随源码发布。

## 自动验证

- 核心测试覆盖路由选择、身份确认、切号顺序、取消与并发保护、VDF 处理和游戏库合并。
- 数据导出测试覆盖普通/沙盒缓存去重、未知与零值、成就明细完整性、缓存冲突、损坏数据、CSV 转义/公式处理、JSON 字段、账号隔离及取消清理。
- Millennium 插件测试覆盖路由、详情页按钮、配置解码、游戏库采集和备用入口。
- Windows 文件集成测试在临时目录检查快捷方式安装/删除、备份、幂等性、损坏保护、中文路径和命令行引号；运行前需正常退出 Steam。

## 已进行的 Windows 人工验证

开发过程中验证过双账号普通/沙盒并行登录、旧版通过 Watt 商店版完成的双向切号、CrossCode 原生详情页路由及沙盒启动、Sudoku Universe 公共库下载、设置界面交互、时长与成就导出，以及托盘关闭窗口/恢复/退出和纯后台启动。上述是特定环境下的结果，不代表所有游戏和所有依赖版本均兼容。

设置窗口的搜索、游戏选择、导入窗口和默认/最小尺寸已检查。托盘图标在隐藏图标区域可见，双击和菜单打开设置；退出后台后图标移除，重复启动保持单实例。未测试 Explorer 崩溃恢复。

公共库验证检查过沙盒写入的实际落点，未进行多客户端并发更新或破坏性写入测试。导出仅验证本地缓存转换，不代表服务器数据完整。测试不自动代替用户确认 Steam Guard、账号身份或云存档。

## 已知限制

Millennium 3.4.1 在测试沙盒环境内发生启动异常，当前脚本在沙盒内阻止其 Steam 主加载器与 CEF 网页加载器；因此沙盒实时插件信号与原生按钮适配不可用。Steam 的非公开接口可能变化，所有原生启动入口未逐项验证。没有自动存档迁移、跨客户端下载互斥或可靠的游戏启动完成检测。

复现命令与构建依赖见 README。发布前应重新运行自动测试、构建插件和 Windows 自包含程序；实际登录、游戏兼容性与云存档验证必须在配置好的 Windows 环境中进行。

## 脱敏源码发布检查

本次公开源码副本的核心测试 49/49、插件测试 24/24 通过，Windows App/CLI 自包含交叉发布和插件构建成功。待提交文件通过 Gitleaks 扫描以及个人标识、绝对用户目录与数据文件排除检查；应用 PNG 的非视觉元数据已移除，压缩像素数据保持一致。公开提交使用 GitHub noreply 邮箱。扫描只能覆盖已有规则和已知模式，后续提交仍需人工检查。


## 插件虚拟未安装游戏页

新增开关、安装清单扫描、Millennium 侧栏入口和虚拟封面页面。实现不调用 Steam 收藏夹或快捷方式增删接口；依赖原有启用的路由规则，数据不足时明确提示。图片缺失有横幅及文字回退，图片不经过本项目服务器。

自动验证覆盖账号过滤、禁用规则、本地已安装过滤、安装/卸载后的列表变化、过期和损坏安装清单、名称/AppID 搜索、固定 CDN 地址，以及 DOM 挂载、标题转义、详情和下载回调、账号变化保护、Escape 和卸载清理。Windows 实机确认侧栏入口、封面网格和本地安装索引，原有 shortcuts.vdf 哈希不变；账号、游戏规则和工具路径不变，仅启用新的页面开关。人工账号确认和下载仍由用户完成。

本轮核心测试 49/49、插件测试 31/31 通过，App/CLI 自包含发布和插件打包成功。用户在实机从卡片详情点击下载入口，完成账号确认后，已验证打开另一账号的对应游戏页；该验收未要求实际下载。


## 虚拟页面滚动自动加载

手动“显示更多”改为滚动接近底部自动追加 60 款，保留已有 DOM、封面、焦点、详情和滚动位置。滚动事件合并处理，关闭页面或卸载插件时取消待处理任务；搜索重置分页和滚动位置，账号变化或安装状态不可靠时停止追加。插件测试 32/32 通过，新增 DOM 测试覆盖连续滚动、到达全部、完整列表搜索、搜索期间取消旧任务、账号变化和清理。插件打包成功。本轮未改动路由、下载和安装状态扫描逻辑。


## 沙盒 Steam 服务与页面加载

沙盒创建脚本在检测到系统已安装 Steam Client Service 时，添加 `StartService=Steam Client Service`，让服务在各自沙盒内运行；保留现有启动服务项，不开放宿主服务管道。缺少服务时给出提示，不安装或修改宿主服务。该设置需要沙盒进程完全退出后重新启动才生效，配置修改前仍保留备份。

本机修复前，沙盒日志反复出现 `Failed to create Service pipe` / `Failed to connect to Steam Service`，进程检查只有宿主服务。补上设置并正常重启小号后，确认沙盒内独立服务存活，验证期间未新增这些连接错误，大号进程保持运行。页面截图确认封面、库图标和成就区域可显示。

此步骤仅修复了服务连接问题：重启后的首次库加载仍有空白等待，连续打开两个游戏页从导航指令到 `Loading content` 日志约 6–8 秒。随后核对 Steam 前端代码，确认该日志仅指社区内容区域，不是整个页面开始加载或渲染完成的计时，不能用来证明封面加载速度。未清空账号、网页缓存或存档，未放宽账号数据隔离。设置依据见 [Sandboxie StartService 文档](https://sandboxie-plus.com/sandboxie/startservice/) 和 [Steam 沙盒性能问题记录](https://github.com/sandboxie-plus/Sandboxie/issues/4161)。

## 沙盒本地资源半秒等待修复

通过临时启用小号 CEF 的本机调试接口，直接测量资源请求与界面长任务。修复前，同一封面文件在沙盒内直接读取仅需几毫秒，但通过 `steamloopback.host` 请求图片、字体和脚本时，每批约四个请求固定等待约 500 毫秒；切页时排队累积数秒，JavaScript 心跳仍能正常响应。

进程模块检查发现只阻止主进程 `wsock32.dll` 并未禁用网页进程中的 Millennium `VERSION.dll`。补充仅针对 `steamwebhelper.exe` 和该 DLL 完整路径的阻止规则后，网页进程改用 Windows 系统 `VERSION.dll`。脚本枚举 Steam 的 CEF 子目录，并通过文件描述确认组件属于 Millennium，避免阻止其他同名 DLL。添加规则复用已有去重逻辑，原有规则保留，配置修改前备份。

同机同会话复测：两个封面的单次本地请求从约 507–508 毫秒降至约 1–2 毫秒；相同导航场景中匹配到的 38 个本地图片请求，中位完成耗时从约 4689 毫秒降至约 36 毫秒，游戏主横幅从约 4534 毫秒降至约 7 毫秒。截图确认库图标、横幅和成就显示正常。这些数值是本地资源请求耗时，不是整页加载时间，也不代表外网社区内容或所有设备的性能。诊断期间尝试的 `SysCallLockDown` 没有改善，已撤回；保留此前的 Steam 服务修复。

Windows PowerShell 语法解析通过；实机识别两个 CEF 目录中的 Millennium 加载器，并确认 Windows 系统 DLL 不匹配。交付脚本连续执行两次，第二次配置哈希保持不变。结束诊断后以普通参数重启小号，确认本机调试端口关闭、小号网页进程仅加载系统 VERSION.dll、大号原有 Millennium 主组件和网页组件仍在运行。


## 移除 Watt Toolkit 依赖

账号选择由内置实现替代，移除商店激活、安装检测、路径设置及配置字段。核心测试覆盖原样保留 VDF 非目标内容、缺失标记补齐、重复操作、SteamID/用户名核对、损坏与重复字段拒绝，以及旧 `wattExe` 配置兼容；原有进程退出、云同步确认、并发与实际账号检查仍保留。

Windows 账号选择集成测试通过 `--account-selection` 单独运行，只使用临时文件和临时注册表键，可在 Steam 运行时执行。覆盖原文件备份、重复执行、运行进程保护、并发文件修改、注册表写入失败后恢复，以及错误后的临时文件清理。此前 Watt 的实机切号验收不代表新内置实现已完成实际登录验收；Steam Guard、真实账号双向切换和云存档确认仍需用户在界面完成。

本轮核心测试 54/54 通过，Windows 临时文件/注册表集成测试通过，本机两个配置账号的只读选择预检通过（未修改真实 Steam 登录文件或注册表）。App/CLI Windows 自包含发布成功；升级仅删除旧 Watt 路径字段，账号和游戏路由保持一致。当前沙盒配置未启用诊断期间临时尝试的 SysCallLockDown，内置切号不增加 Sandboxie 赞助功能依赖。
